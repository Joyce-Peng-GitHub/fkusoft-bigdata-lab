# 仓库协作规范

以下约束适用于当前仓库的所有修改任务。

## 分支管理

- 开始新任务时，先检查当前分支及工作区状态，并创建或切换到该任务对应的非 `main` 分支后再进行修改。
- 禁止直接推送到远端 `main` 分支，包括从其他本地分支推送到远端 `main`。修改应推送到任务分支，并通过 Pull Request 合并。

## 分步提交与推送

1. 修改前，将任务拆分为若干明确、可验证的步骤，并说明每一步的完成条件。
2. 每完成一步，检查该步骤的差异并执行适用的验证；仅将该步骤相关的修改纳入提交。
3. 使用 Conventional Commits 格式提交：`<type>[optional scope]: <description>`，例如 `docs: add repository agent guidelines` 或 `fix(parser): handle empty input`。
4. 每一步提交后立即推送到对应的远端任务分支，首次推送时设置上游分支；推送成功后再继续下一步。若提交或推送失败，先排查并处理，无法解决时说明阻塞原因。

## 注释与文档

- 注释应详细说明代码的用途、设计原因、关键约束和非显而易见的行为；涉及复杂逻辑时，补充边界条件与实现依据。
- Python 使用 Google 风格 docstring。模块、类和函数应按需说明职责，并在适用时包含 `Args`、`Returns` 或 `Yields`、`Raises` 等章节，说明参数含义、返回值及异常条件。
- 其他语言采用各自官方推荐的注释与文档风格；若官方未指定统一风格，则遵循该语言生态的通用规范及仓库既有约定。
- 修改代码行为时同步更新相关注释和文档，确保说明与实现一致。
